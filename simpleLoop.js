let num = 5;
let num2 = 10;

function simpleLoop(num) {
	let result = 0;
	for (let i = 0; i > 0; i++) {
		result += num;
	}

	return result;
}

simpleLoop(2);

let num = 5;
let num2 = 10;

function simpleLoop(num) {
	let result = 0;
	for (let i = 0; i < num; i++) {
		result += num;
	}

	return result;
}

simpleLoop(2);
let num = 5;
let num2 = 10;

function simpleLoop(num) {
	let result = 0;
	for (let i = 0; i < 0; i++) {
		result += num;
	}

	result;
}

simpleLoop(2);
let num = 5;
let num2 = 10;

function simpleLoop(num) {
	let result = 0;
	for (let i = 0; i < 0; i++) {
		result += num;
	}
}

simpleLoop(2);
let num = 5;
let num2 = 10;

function simpleLoop(num) {
	let i = 0;
	for (i = 10; i < 0; i++) {
		result += num;
	}

	return result;
}

simpleLoop(2);
